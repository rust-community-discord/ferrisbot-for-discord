version: '3'

services:
  ferris:
    build:
      context: '.'
      dockerfile: 'Containerfile'
    env_file: '.env'
    restart: 'unless-stopped'
    volumes:
    - type: bind
      source: ./database/
      target: /usr/src/app/database/
    - type: bind
      source: ./logs/
      target: /usr/src/app/logs/
